import { HighlightUpdateDirective } from './highlight-update.directive';

describe('HighlightUpdateDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightUpdateDirective();
    expect(directive).toBeTruthy();
  });
});
